ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first. Some steps
      require Computer Use; fall back to a Playwright headed script if
      unavailable.
SCOPE: One PR. Documentation + small fixes (each capped at 20 lines).

# Prompt 5.9 — Manual accessibility sweep (NVDA + VoiceOver)

**Goal:** Catch the issues automated tools miss: keyboard traps, focus
order on language toggle, screen-reader announcements at stage boundaries.

**Audit ref:** `audits/02-uiux.md` U-2 / U-6 / U-9.
**Depends on:** P5 wave-1 fully merged (the skip-link, error policy,
auto-advance, Likert label, intro CTAs are all in place).

## TODO

1. Write `docs/A11Y-SWEEP-2026-05.md` with sections:
   - Tools used (browser, OS, SR)
   - Routes tested (`/`, `/s/phase-1`, `/admin`)
   - Each scenario: Steps / Expected / Actual / Severity (Blocker /
     Serious / Polish)
2. Run each scenario:
   - Tab order from `/` → reach Start without losing focus.
   - Language toggle: focus stays on the toggle or returns to the
     previously focused field.
   - Survey advance: SR announces new question text within 1s.
   - Save & Exit: SR announces the new mode + the resume link is
     focusable.
   - Error banner: SR announces "alert" + the error message.
   - Admin login error: SR announces the lockout message.
3. For every Blocker/Serious found, ship a ≤ 20-line fix in this PR.
   Polish issues become follow-up issues with `audits/02-uiux.md`
   ref + this sweep doc line ref.
4. Do not deepen the scope — if you find a 100-line problem, file an
   issue and stop.

## Verification

- `docs/A11Y-SWEEP-2026-05.md` exists and lists each scenario with a
  pass/fail outcome.
- `bun run test` green.
- `bunx --bun @axe-core/cli http://localhost:5173/ --exit` returns 0.

## Commit & PR

- Branch: `a11y/manual-sweep-2026-05`
- Commit: `a11y: manual NVDA + VoiceOver sweep + targeted fixes [5.9]`
- PR body lists the fixes and links to follow-up issues for Polish
  findings.

## Stop conditions

- No screen reader available → STOP and ask the human to do the sweep
  manually using this doc as a template.
- Any Blocker fix exceeds 20 lines → file an issue, defer to P6 polish.
