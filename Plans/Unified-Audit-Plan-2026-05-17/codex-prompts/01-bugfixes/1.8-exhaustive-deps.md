ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first.
SCOPE: One PR. ≤ 10 lines diff.

# Prompt 1.8 — Resolve `react-hooks/exhaustive-deps` in SurveyRunner

**Goal:** Clear the dep-array warning at `SurveyRunner.tsx:~448` either
by adding the missing dep or by documenting the deliberate omission.

**Audit ref:** `audits/06-code-quality.md` Q-2.

**Depends on:** P1 wave-1 fully merged (1.3 in particular settled the
motion logic; the offending `useEffect` may or may not still exist as
written).

## TODO

1. Run `bun run lint src/components/SurveyRunner.tsx 2>&1 | grep -A2 exhaustive-deps`
   to find the current line + the missing dep name.
2. Read 50 lines around that line.
3. Choose **one** option:
   - **Add the dep** — preferred. Verify the effect doesn't run more than
     necessary by reading the body; if adding the dep causes loops, see
     option 2.
   - **Document the omission** — add an `eslint-disable-next-line
     react-hooks/exhaustive-deps` with a one-line justification, e.g.
     `// intentional: only re-run when stage changes; <ref> is read via
     ref to avoid stale-closure flicker`. The justification MUST cite a
     concrete reason, not "intentional".
4. Do NOT introduce a new ref or rewrite the effect. This is a surgical
   fix.

## Verification

```bash
bun run lint src/components/SurveyRunner.tsx 2>&1 | grep exhaustive-deps && exit 1
bun run typecheck
bun run test src/components/__tests__/SurveyRunner
```

The grep must find nothing for the verification to pass.

## Commit & PR

- Branch: `chore/surveyrunner-exhaustive-deps`
- Commit: `chore(SurveyRunner): resolve exhaustive-deps at line N [1.8]`
- PR body refs `audits/06-code-quality.md` Q-2 and notes Option A vs B.

## Stop conditions

- If the warning is gone (because 1.3 incidentally fixed it), commit a
  comment-only PR noting "1.8 obsolete after 1.3" and close.
- If adding the dep introduces a render loop in the test suite, STOP
  and revert to Option 2.
