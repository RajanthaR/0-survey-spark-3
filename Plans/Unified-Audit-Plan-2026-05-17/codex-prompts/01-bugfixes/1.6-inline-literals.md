ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first.
SCOPE: One PR. Touches 5 source files + the i18n dictionary.

# Prompt 1.6 — Route inline SI/TA literals through dictionaries

**Goal:** Drop all 52 `no-restricted-syntax` violations by moving inline
Sinhala/Tamil literals into approved dictionary files (or expanding the
ESLint allowlist for genuine dictionary modules).

**Audit refs:** `audits/01-content.md` C-2, `audits/08-i18n.md` I-2.

**Depends on:** 1.8 merged (avoids SurveyRunner merge conflicts) and 1.7
merged (parity test exists to catch missing translations).

## TODO

1. Read first:
   - `src/lib/i18n.tsx` — find the `UI` export shape
   - `src/lib/__tests__/no-inline-localized-labels.test.ts`
   - `src/lib/__tests__/no-hardcoded-question-strings.test.ts`
   - `eslint.config.js` — find the `no-restricted-syntax` allowlist
   - `src/components/SurveyRunner.tsx:510-525` (the "+N skipped" pill)
   - `src/components/survey/ResponseVisualSummary.tsx:1-40`
   - `src/lib/analytics-report-i18n.ts:1-60`
   - `src/lib/exports-extended.ts:70-90`
   - `src/routes/admin.tsx:2460-2480`
2. Add to `src/lib/i18n.tsx` `UI`:
   - `UI.skipped: { en: "Skipped", si: "මඟ හැරීමි", ta: "தவிர்க்கப்பட்டது" }`
   - `UI.englishLabel: { en: "English", si: "ඉංග්‍රීසි", ta: "ஆங்கிலம்" }`
   - Any other missing keys discovered by 1.7's parity test.
3. Edit `src/components/SurveyRunner.tsx:~517` to render
   `pickText(UI.skipped, lang)` instead of the inline literal.
4. Edit `src/components/survey/ResponseVisualSummary.tsx:~19` to use the
   `UI` lookup or a local module-top dictionary `{ en, si, ta }`.
5. Edit `src/lib/analytics-report-i18n.ts`:
   - Decide between (a) moving the strings into `UI`, or
     (b) **renaming this file** to `src/lib/i18n/analytics-report.ts`
     and adding the new path to the ESLint allowlist.
   - Default to **(b)** — this file *is* a dictionary; the lint rule was
     wrong to flag it. Update `eslint.config.js` allowlist accordingly.
6. Edit `src/lib/exports-extended.ts:~79`. If the literal is in a code
   comment, replace with a placeholder string (`"[SECTION 1] role"`).
   If it is runtime code, move to a dedicated export-label dictionary.
7. Edit `src/routes/admin.tsx:~2468` (the language label site) to use
   `pickText(UI.englishLabel, lang)` or equivalent.
8. Run the parity test from 1.7; fix any new missing keys.

## Verification

```bash
bun run test src/lib/__tests__/no-inline-localized-labels.test.ts
bun run test src/lib/__tests__/no-hardcoded-question-strings.test.ts
bun run test src/lib/__tests__/i18n-parity.test.ts
bun run lint src/components src/lib src/routes/admin.tsx 2>&1 | grep no-restricted-syntax && exit 1
bun run typecheck
bun run test                                # 0 failures
```

The grep must find nothing.

## Commit & PR

- Branch: `i18n/route-inline-literals`
- Commit: `i18n: route remaining SI/TA literals through dictionaries [1.6]`
- PR body refs `audits/01-content.md` C-2 and `audits/08-i18n.md` I-2.
- Includes a bullet list of every literal moved + new dictionary key.

## Stop conditions

- If `analytics-report-i18n.ts` cannot be cleanly moved (e.g. its tests
  import from the old path in many places), default to option (a) and
  inline its strings into `UI`.
- If `bun run test` failure count increases, STOP and bisect.

## What this unlocks

After 1.6 lands, the CI lint job in `pr.yml` can drop its
`continue-on-error: true` overlay (added in 0.3). Open a tiny follow-up
PR to remove that line.
