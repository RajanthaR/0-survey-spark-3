ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first.
SCOPE: Multiple stacked PRs — one per panel. NOT a single 4 000-line PR.
MODEL: Use **GPT-5.5 Pro**, not Thinking. The diff is wide and the cost
       of a wrong call dwarfs the call cost.
LANES: 1 agent. Do not parallelise the panels — the route file's import
       block changes with every panel move.

# Prompt 3.2 — Split `src/routes/admin.tsx` (XL)

**Goal:** `src/routes/admin.tsx` becomes a ≤ 200-line layout file. Each
feature lives as a child route under `src/routes/admin/*` OR as a
focused panel under `src/components/admin/*`. **Behaviour is identical**;
the regression target is the screenshot tree.

**Audit refs:** `audits/05-architecture.md` A-2, `audits/03-performance.md` P-1.
**Codex-audits ref:** `06-architecture-audit.md`.

**Depends on:** 3.1 (screenshot baseline) + P3 wave-1 merged.

⚠ **DEEP-LINK CHANGE RISK**: today admin uses query params for tab
state (e.g. `/admin?tab=exports`). The split converts these to
`/admin/exports`. If the human wants the old query-param URLs to keep
working, add redirects (planned).

## TODO (do not deviate)

### Phase A — Discovery (no edits)

1. `wc -l src/routes/admin.tsx`     → confirm ≈ 4 099.
2. Read the entire file in chunks of 600 lines.
3. Identify feature panels. Likely list:
   - Auth bootstrap + login
   - Filters + survey selector
   - Stats overview cards + dropoff chart
   - Response list (with pagination)
   - Response detail drawer
   - Exports (all-valid, filtered, codebook, ZIP, validation)
   - Reports (analytics report panel)
   - Alerts panel
   - Live status
4. List every test under `src/components/admin/__tests__` and
   `src/routes/__tests__` that touches the admin route. These are your
   regression contract.
5. Read `src/routes/admin/charts-lazy.tsx` (existing split pattern) and
   `src/routes/admin/LoginPanel.tsx` (existing extraction).
6. Write a one-page plan to `Plans/Unified-Audit-Plan-2026-05-17/admin-split-plan.md`
   enumerating: panel name → target path → estimated line range in the
   current `admin.tsx`.

### Phase B — Per-panel extraction (one PR per panel)

For each panel, in this order:

1. **Filters & survey selector** → `src/routes/admin/filters/FiltersPanel.tsx`
2. **Stats overview** → `src/routes/admin/stats/StatsPanel.tsx`
3. **Response list** → `src/routes/admin/responses/ResponseListPanel.tsx`
4. **Response detail** → `src/routes/admin/responses/ResponseDetailPanel.tsx`
5. **Exports** → `src/routes/admin/exports/ExportsPanel.tsx`
6. **Reports** → `src/routes/admin/reports/ReportsPanel.tsx`
7. **Alerts** → `src/routes/admin/alerts/AlertsPanel.tsx`
8. **Live status** → `src/routes/admin/LiveStatusPanel.tsx`

For each PR:

- Move the JSX + immediate pure helpers into the new file.
- Convert prop-drilled state to a typed context if it crosses more than
  3 components; otherwise keep prop-drilling.
- Pure helpers (`computeDropoff`, etc.) move to `src/lib/admin/<area>.ts`
  in a follow-up (prompt 3.6 handles this).
- `admin.tsx` imports the new file and renders it where the old block
  was.
- Run `bun run test` after every PR. Any red → revert that PR.

### Phase C — Convert tabs to child routes (last PR of the chain)

1. Add `src/routes/admin/index.tsx` (default `/admin`) showing the
   stats panel.
2. Add `src/routes/admin/$tab.tsx` mapping
   `exports|responses|reports|alerts|filters|live` to the panel.
3. Add a small migration redirect for the old query-param URLs:
   `/admin?tab=exports` → `/admin/exports`.
4. Update internal `<Link>`s to use the new URL shape.
5. `admin.tsx` becomes a layout file: auth check + sidebar +
   `<Outlet />`.

### Phase D — Cleanup

1. Verify `wc -l src/routes/admin.tsx` ≤ 200.
2. Verify each new panel file ≤ 500 lines.
3. Re-run `bun run screenshots` into
   `Plans/Unified-Audit-Plan-2026-05-17/screenshots/post-p3-2/`.
4. Eyeball-diff against `pre/`; only allowed differences are motion
   timing.
5. Record bundle-size diff (after 4.1's `size-limit` lands, but you can
   also capture from `bun run build` output now): admin route shell
   should drop by ≥ 100 KB.

## Verification (every PR)

```bash
bun run typecheck
bun run lint
bun run test                       # 100% pass — no skips
bun run build                      # exits 0
wc -l src/routes/admin.tsx         # decreasing every PR
```

## Verification (final)

```bash
wc -l src/routes/admin.tsx                                # <= 200
find src/routes/admin -name '*.tsx' | xargs wc -l | sort  # each <= 500
bun run screenshots
# Compare pre/ vs post-p3-2/ manually or via image-diff tool.
```

## Commit & PR

Branch per phase:
- `refactor/admin-split-a-filters`
- `refactor/admin-split-b-stats`
- `refactor/admin-split-c-responses-list`
- `refactor/admin-split-d-responses-detail`
- `refactor/admin-split-e-exports`
- `refactor/admin-split-f-reports`
- `refactor/admin-split-g-alerts`
- `refactor/admin-split-h-live`
- `refactor/admin-split-i-child-routes`

Commit subject template:
`refactor(admin): extract <panel> into <path> [3.2.<n>]`

PR body for every PR:
```
Plan ref: Plans/Unified-Audit-Plan-2026-05-17/05-phase-3-architecture.md
Audit ref: audits/05-architecture.md A-2
Prompt: codex-prompts/03-architecture/3.2-admin-split.md
Panel: <name>
Lines moved: <count>
Tests touched: <list>
admin.tsx lines: <old> -> <new>
```

## Stop conditions (any of these → STOP and escalate)

- A test goes red.
- The diff for a single panel exceeds 800 lines.
- The agent finds itself rewriting a hook signature (that's 3.3's job).
- The agent finds itself moving a server fn (that's not in scope).
- More than 30 minutes elapse without progress on a panel.
- `bun run typecheck` reports any cross-file regression.

## What this unlocks

- 3.4 (type filter chain) — now has stable `applyFilters` callers.
- 3.6 (move pure functions) — now has clearly separated panels.
- 4.1 (size-limit) — now has measurable per-panel chunks.
- 5.* (UX polish) — now has small panel files to edit.
