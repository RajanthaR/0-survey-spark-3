ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first.
SCOPE: One PR. Adds tooling + a screenshots tree. No source edits.
TOOLS: This prompt uses Codex Desktop's **Computer Use** + in-app
       browser. If you only have CLI access, fall back to Playwright
       headed mode (instructions below).

# Prompt 3.1 — Pre-refactor screenshot baseline

**Goal:** Capture deterministic visual + DOM baselines for `/`,
`/s/phase-1`, and `/admin` in EN / SI / TA before P3.2 + P3.3 rewrite
internals. These screenshots are the regression target — every later
P3 / P5 prompt diffs against them.

**Plan ref:** `Plans/Unified-Audit-Plan-2026-05-17/05-phase-3-architecture.md` Session 3.1.

## TODO

1. Create the screenshot tree:
   ```
   Plans/Unified-Audit-Plan-2026-05-17/screenshots/
   ├── pre/
   │   ├── en/
   │   ├── si/
   │   └── ta/
   ├── post-p3-2/
   ├── post-p3-3/
   └── post-p5/
   ```
   Only `pre/` is populated by this prompt.
2. Add `scripts/capture-screenshots.mjs` that:
   - Boots `bun run dev` on port 5173 if not running.
   - Uses Playwright (already a dev dep) to navigate each route in
     each language at viewports 360×800, 768×1024, 1280×800.
   - Saves PNGs to `Plans/Unified-Audit-Plan-2026-05-17/screenshots/pre/<lang>/<route>-<viewport>.png`.
   - Also dumps a serialized DOM snapshot (`outerHTML`) to a `.html`
     sibling next to each PNG.
3. The script must be deterministic:
   - Set `prefers-reduced-motion: reduce`.
   - Disable network animations via CSS injection
     (`* { animation: none !important; transition: none !important; }`).
   - Wait for `networkidle` + 500 ms before screenshotting.
4. The routes to capture:
   - `/` (home)
   - `/s/phase-1` — at intro, after consent, at Q1, at Q3, at review,
     at contact, at done. Use the "Next" button to advance; seed Q1-Q3
     with deterministic answers via a helper.
   - `/admin` — login screen only (without credentials).
5. Add a `bun run screenshots` script in `package.json`.
6. Run the script and commit the resulting tree (yes — committing PNGs
   is intentional; they are documentation, not assets).
7. Add a `.gitattributes` rule so the PNGs use `binary` diff handling.

## Verification

```bash
bun run screenshots
ls Plans/Unified-Audit-Plan-2026-05-17/screenshots/pre/en/ | wc -l   # expect >= 21
ls Plans/Unified-Audit-Plan-2026-05-17/screenshots/pre/si/ | wc -l
ls Plans/Unified-Audit-Plan-2026-05-17/screenshots/pre/ta/ | wc -l
```

## Commit & PR

- Branch: `ops/screenshot-baseline`
- Commit: `ops(screenshots): pre-P3 baseline for EN/SI/TA × 3 viewports [3.1]`
- PR body refs `Plans/Unified-Audit-Plan-2026-05-17/05-phase-3-architecture.md`
  Session 3.1 and lists the route × language matrix.

## Stop conditions

- If Playwright cannot start (browsers not installed): run
  `bunx playwright install chromium` and retry. If still failing, STOP
  and report.
- If a route consistently fails to render in one language (real bug?),
  STOP and report; do not commit empty screenshots.
- Total PNG payload should be ≤ 5 MB. If it grows beyond, downscale to
  WebP at quality 80.
