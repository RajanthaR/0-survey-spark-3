ROLE: You are a senior engineer on survey-spark-3.
MODE: Execute the TODO below. Do not propose a plan first. Do not
      summarise the codebase. Start with step 1 immediately.
SCOPE: One PR. No scope creep. If a step expands beyond the listed
       files, STOP and report.

# Prompt 0.1 — Reproducible install + scripts

**Goal:** `bun install && bun run typecheck && bun run lint && bun run test`
runs cleanly on a fresh clone, with a documented fallback for environments
where Bun is missing.

**Plan ref:** `Plans/Unified-Audit-Plan-2026-05-17/02-phase-0-tooling.md` Session 0.1
**Audit refs:** `audits/06-code-quality.md` Q-7 / Q-9, `audits/09-tech-stack.md` St-6, `audits/13-codex-parity-supplement.md` S-19 / S-20

## TODO

1. Read these files first (no edits yet):
   - `package.json`
   - `bunfig.toml`
   - `README.md`
   - `.prettierignore` (if it exists)
   - `audits/13-codex-parity-supplement.md` lines covering S-19 / S-20
2. Add scripts to `package.json` (do not remove existing scripts):
   - `"typecheck": "tsc --noEmit"`
   - `"lint:fix": "eslint . --fix"`
   - `"format:check": "prettier --check ."`
3. Create `.nvmrc` containing the major Node version the CI uses
   (`24` if unset; otherwise read from CI config).
4. Create `.tool-versions` with:
   ```
   bun 1.2.x        # match the version in CI
   nodejs 24.14.1   # or whatever .nvmrc says
   ```
5. Create `.gitattributes` with:
   ```
   src/integrations/supabase/types.ts linguist-generated=true
   src/routeTree.gen.ts linguist-generated=true merge=union
   ```
6. Create `docs/TROUBLESHOOTING.md` covering:
   - `bun: command not found` → install Bun, OR run binaries via
     `node ~/.nvm/versions/node/v<MAJOR>.x.x/bin/node ./node_modules/<pkg>/bin/<bin>.js`
   - Rollup `@rollup/rollup-darwin-arm64` native binary code-signing
     failure on macOS → run `bun install --force` or `npm rebuild
     @rollup/rollup-darwin-arm64`
   - `pnpm audit` → not supported; this repo uses Bun's lockfile.
   - The Node fallback path pattern.
7. Add a "Troubleshooting" section near the top of `README.md` that
   links to the new file.

## Verification

```bash
bun run typecheck                # exits 0
bun run lint:fix                 # changes only whitespace
bun run format:check             # exits 0 after lint:fix
test -f .nvmrc && test -f .tool-versions && test -f .gitattributes
test -f docs/TROUBLESHOOTING.md
grep -q "TROUBLESHOOTING" README.md
```

All commands must exit 0.

## Commit & PR

- Branch: `ops/reproducible-install`
- Commit: `ops(tooling): reproducible install scripts + TROUBLESHOOTING [0.1]`
- PR body must include:
  ```
  Plan ref: Plans/Unified-Audit-Plan-2026-05-17/02-phase-0-tooling.md
  Audit ref: audits/13-codex-parity-supplement.md S-19
  Prompt: codex-prompts/00-tooling/0.1-reproducible-install.md
  ```

## Stop conditions

- If `bun install` itself is broken in this environment, STOP and ask
  the human to investigate (this prompt assumes a working install).
- If `package.json` already has a `typecheck` script with different
  semantics, STOP and ask which to keep.
- Do **not** delete or move `bun.lock`; do **not** add `pnpm-lock.yaml`
  or `package-lock.json`.
